/**
 * Infrastructure layer.
 *
 * <p>Adapters for persistence (JPA + pgvector), messaging (RabbitMQ), AI (Ollama bridge),
 * tools (Filesystem/Git/Docker), and web (REST + WebSocket).
 */
package dev.skymetron.infrastructure;
