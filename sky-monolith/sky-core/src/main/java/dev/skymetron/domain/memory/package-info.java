/**
 * BC 2 — Memory.
 *
 * <p>What the system knows: Facts, MemoryEntry, Embedding, VaultDocument.
 *
 * <p>Persisted in PostgreSQL + pgvector. Uses DDD Aggregates for versioning and persistence.
 */
package dev.skymetron.domain.memory;
